project:
  name: aws-quickstart-workshop
  regions:
  - ap-southeast-1
  - us-east-1
  - us-west-2
  - eu-central-1
tests:
  default:
    template: ./templates/master.template.yaml
    regions:
    - us-west-1    
    parameters:
      AvailabilityZones: "$[taskcat_getaz_2]"
      EmailAddress: mirakapaolo@gmail.com
      KeyPairName: paolo
      WebserverCIDR: "0.0.0.0/0"
      QSS3KeyPrefix: "aws-quickstart-workshop/"
      QSS3BucketRegion: "$[taskcat_current_region]"
      QSS3BucketName: "$[taskcat_autobucket]"

